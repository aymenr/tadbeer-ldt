import React, { Component } from 'react';

export default class Statement extends Component {

  constructor(props) {
    super(props);
  }

  setFocus = (focus) => {
    this.setState({ focus })
  }

  render() {
    return false;
  }
}
